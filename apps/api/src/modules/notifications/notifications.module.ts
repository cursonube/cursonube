import { Module } from '@nestjs/common';

/**
 * Emails transaccionales (bienvenida, invitación, reset de contraseña,
 * notificación de Lead) detrás de una interfaz EmailProvider (Resend en MVP).
 * Documento 18 (adenda), sección 2.
 */
@Module({})
export class NotificationsModule {}
